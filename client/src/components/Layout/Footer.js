import React from "react";

const Footer = () => {
  return (
    <footer className="flexCenter">
      RevMe Copyright &copy; {new Date().getFullYear()}
    </footer>
  );
};

export default Footer;
